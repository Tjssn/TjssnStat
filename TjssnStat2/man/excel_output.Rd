% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/super_param_excel_out.R
\name{excel_output}
\alias{excel_output}
\title{excel_out}
\usage{
excel_output(Object, Path = NULL, name = NULL)
}
\arguments{
\item{Object}{一个super_param对象。必须包含output$result$excel_output$body（待导出的数据）。}

\item{Path}{字符串。Excel 文件保存的目录路径。默认为当前工作目录。}

\item{name}{字符串。Excel 文件的名称（可包含或不包含.xlsx扩展名）。默认规则：
\itemize{
\item 优先使用Object$output$result$excel_output$file_name（若存在）；
\item 若未指定名称，默认使用default_output.xlsx。
}}
}
\value{
不可见地返回生成的 Excel 文件的完整路径（字符串）。若导出失败，则返回NULL。
}
\description{
此函数将super_param对象中存储的结构化结果（位于output$result$excel_output$body）导出到 Excel 文件。
支持自定义文件路径和名称，并自动处理已存在的文件（例如，添加时间戳以避免覆盖）。
}
\details{
函数执行以下步骤：
\enumerate{
\item 确定输出目录（Path）：若未指定，则使用当前工作目录。
\item 生成输出文件名（name）：
}
\itemize{
\item 若存在Object$output$result$excel_output$file_name，则使用该名称；
\item 若未提供名称，默认使用default_output.xlsx。
}
\enumerate{
\item 检查是否存在同名文件并处理冲突（交互模式下询问用户是否覆盖 / 重命名 / 中止）。
\item 若输出目录不存在，则创建该目录。
\item 使用openxlsx包将Object$output$result$excel_output$body中的数据写入 Excel 文件。
}
}
\note{
\itemize{
\item 依赖openxlsx包。若未安装，需先执行install.packages("openxlsx")。
\item 输入Object必须包含output$result$excel_output$body（一个数据框列表，每个元素对应 Excel 中的一个工作表）。
\item 若输出文件已存在：
\item 交互模式下（如 RStudio），会提示用户选择覆盖、重命名（添加时间戳）或中止。
\item 非交互模式下，文件会自动添加时间戳（YYYYMMDD_HHMMSS）重命名。
\item 时间戳使用系统本地时间。
}
}
\seealso{
\itemize{
\item excel_in：从 Excel 导入配置数据。
\item super_param：生成包含待导出结果的super_param对象。
}
}
\author{
TjssnStat 开发团队 tongjibbb@163.com
}
\keyword{Excel}
\keyword{导出}
\keyword{数据写入}
\keyword{结果}
